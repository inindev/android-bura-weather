<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ Copyright 2024 David Takač
  ~
  ~ This file is part of Bura.
  ~
  ~ Bura is free software: you can redistribute it and/or modify it under the terms of the GNU General Public License as published by the Free Software Foundation, either version 3 of the License, or (at your option) any later version.
  ~
  ~ Bura is distributed in the hope that it will be useful, but WITHOUT ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE. See the GNU General Public License for more details.
  ~
  ~ You should have received a copy of the GNU General Public License along with Bura. If not, see <https://www.gnu.org/licenses/>.
  -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required to launch the system clock via ACTION_SHOW_ALARMS (e.g. Samsung Clock). -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <!-- Re-arm the widget refresh alarm after a reboot (alarms don't survive one). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
      Fire the widget refresh tick on an exact allow-while-idle alarm so it holds its cadence in
      Doze instead of drifting hours late. USE_EXACT_ALARM (API 33+) is auto-granted at install;
      minSdk is 35, so no SCHEDULE_EXACT_ALARM fallback is needed. Bura is not on Play Store, so
      its exact-alarm-use policy does not apply.
    -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!--
      Package visibility (Android 11+): the widget fires implicit intents at other apps
      (the system clock and a calendar). Without these <queries>, those intents silently
      fail to resolve under our app's identity, even though the targets exist.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.SHOW_ALARMS" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.APP_CALENDAR" />
        </intent>
    </queries>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".widget.WeatherWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/weather_widget_info" />
        </receiver>

        <!-- Re-arms the widget refresh alarm on boot. Exported so the system can deliver
             BOOT_COMPLETED; it only re-schedules the alarm and ignores other actions. -->
        <receiver
            android:name=".widget.WidgetBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Serves the rendered forecast PNG from the cache to the share sheet's target app. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
